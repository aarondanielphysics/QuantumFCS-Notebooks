# Notebooks

This directory is reserved for companion notebooks for the QuantumFCS.jl paper.

- `paper_examples/`: notebooks for individual examples developed in the
  publication.
- `usage_examples/`: notebooks that demonstrate package usage patterns from the
  manuscript.

Use the repository root as the working directory and run notebooks with the
checked-in Julia project environment.
